
const socketRoute = document.getElementById("ws-route").value;
const socket = new WebSocket(socketRoute);

//socket.onopen = () => socket.send("test message");

const append = document.getElementById("append0");
append.innerHTML = "hello";










